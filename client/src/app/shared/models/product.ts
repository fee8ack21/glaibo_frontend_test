export interface Product {
    id: string
    name: string
    type: number
    typeName: string
    price: number
    image: string
}

export interface ProductType {
    id: number
    name: string
}